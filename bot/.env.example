# Get a token from @BotFather
TELEGRAM_BOT_TOKEN=

# EthioLocal API (no trailing slash) — same as api PORT, path includes /api/v1
API_BASE_URL=http://localhost:4000/api/v1

# Public web app (product links, registration)
WEB_BASE_URL=http://localhost:3000

# Optional: default map center for ranked/trending when user has no location (Addis Ababa)
DEFAULT_LAT=9.032
DEFAULT_LNG=38.748
