# Local dev: leave unset — Next.js rewrites `/api/*` to http://localhost:4000 (see next.config.ts).
# If the UI and API are on different origins in production, set the API origin (no trailing slash):
# NEXT_PUBLIC_API_ORIGIN=https://api.yourdomain.com
