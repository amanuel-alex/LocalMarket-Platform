# Local dev: leave unset — Next.js rewrites `/api/*` to http://localhost:4000 (see next.config.ts).
# Production: point your hosted API or edge proxy; you may use env-driven rewrites / fetch base URL in later modules.
